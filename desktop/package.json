{
  "name": "kingfisher-desktop",
  "version": "1.1.0",
  "private": true,
  "description": "The desktop shell for Kingfisher. Owns the companion, serves the web application, and adds the native file workflows a browser cannot.",
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "pack": "node scripts/build.mjs --dir",
    "dist": "node scripts/build.mjs",
    "dmg:background": "python3 scripts/build-dmg-background.py",
    "dmg:icon": "bash scripts/build-dmg-icon.sh",
    "dmg:assets": "npm run dmg:background && npm run dmg:icon",
    "dmg:verify": "node scripts/verify-dmg.mjs"
  },
  "devDependencies": {
    "electron": "44.2.0",
    "electron-builder": "^26.0.12"
  },
  "author": "Kingfisher",
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
