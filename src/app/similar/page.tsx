import { SimilarWorkspace } from '@/features/similar/SimilarWorkspace';
import { AppShell } from '@/features/shell/AppShell';

export default function SimilarPage() {
  return (
    <AppShell>
      <SimilarWorkspace />
    </AppShell>
  );
}
